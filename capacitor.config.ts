import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.kinggrim.vbtracker',
  appName: 'Volleyball Tracker',
  webDir: 'dist/volleyball-tracker/browser',
};

export default config;
